# Sample media is generated by tests/Streaming/Samples.php (composer samples),
# not committed: it would add megabytes to every clone for files the build can
# reproduce exactly. The directories themselves are kept so the layout is obvious.
in/*
out/*
!.gitignore
!in/.gitkeep
!out/.gitkeep
